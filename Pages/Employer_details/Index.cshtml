@page
@model Employment_advertisement_project.Pages.Employer_details.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Employer_detail[0].Employer_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employer_detail[0].Date_of_establishment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employer_detail[0].Address_of_employer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Employer_detail) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Employer_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date_of_establishment)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address_of_employer)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
