@page
@model Employment_advertisement_project.Pages.Job_details.CreateModel

@{
    ViewData["Title"] = "Create";
}
<!--code to create the database in job deatils table-->

<h1>Create</h1>

<h4>Job_Detail</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Job_Detail.Job_role" class="control-label">Job role</label>
                <input asp-for="Job_Detail.Job_role" class="form-control" />
                <span asp-validation-for="Job_Detail.Job_role" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Job_Detail.Job_type" class="control-label">Job type</label>
                <input asp-for="Job_Detail.Job_type" class="form-control" />
                <span asp-validation-for="Job_Detail.Job_type" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Job_Detail.Job_salary" class="control-label">Job salary</label>
                <input asp-for="Job_Detail.Job_salary" class="form-control" />
                <span asp-validation-for="Job_Detail.Job_salary" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Job_Detail.Job_description" class="control-label">Job description</label>
                <input asp-for="Job_Detail.Job_description" class="form-control" />
                <span asp-validation-for="Job_Detail.Job_description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Job_Detail.Employer_DetailId" class="control-label">Employer Detail Id</label>
                <select asp-for="Job_Detail.Employer_DetailId" class ="form-control" asp-items="ViewBag.Employer_DetailId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
